/*
var config = {};

config.updateEmails = ["update-handler@youremail.com"]; //sends updates about the shows,events scrapped
config.errorEmails =  ["error-handler@youremail.com"]; //sends email if an error has crept in
config.mailgunApi = "key-thisissamplekeymaynotworkgetnew"; //use mailgun api for sending email https://www.mailgun.com/
config.senderEmail = ["from@xyz.com"]; //from email id
module.exports = config;
*/
